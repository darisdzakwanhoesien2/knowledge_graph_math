# Tekniikan matematiikka / Mathematics Division  
**Optimoinnin perusteet (031025A) / Introduction to Optimization**  
**Loppukoe / Exam, 18.1.2016**

### 1. Ratkaise lineaarinen optimointiongelma / Solve the LP problem

$$
\min \; -x_1 + 2x_2 - 5x_3
$$
$$
Ax = b, \quad x \geq 0
$$

missä / where

$$
A = \begin{bmatrix}
-2 & -2 & 1 & -1 & 0 \\
1 & -1 & 2 & 0 & -1 \\
1 & 0 & 0 & 0 & -1
\end{bmatrix}, \quad
b = \begin{bmatrix}
-2 \\
4 \\
-2
\end{bmatrix}.
$$

### 2. Määritä rajoitetun optimointiongelman duaali / Find the dual problem for the constrained optimization problem

$$
\min \; x_1^2 + x_1 x_2 + x_2^2 - 4x_1 + x_2
$$

rajoitteella / subject to the constraint

$$
2x_1 + x_2 - 2 \leq 0.
$$

Ratkaise sekä primaali- että dualiongelma.  
Determine the optimal solution of the primal and dual problem.

### 3. Ratkaise seuraava kvadraattinen optimointiongelma konjugaattigradienttimenetelmällä / Solve the quadratic optimization problem using the conjugate gradient method

$$
\min \; 3x_1^2 - 2x_1 x_3 + 3x_3^2 - x_1 - 3x_2
$$

lähtien alkuarvauksesta / with the initial guess

$$
x^{(0)} = \begin{bmatrix} 0 \\ 0 \\ 0 \end{bmatrix}.
$$

### 4. Määritä Karush–Kuhn–Tucker-ehdot rajoitetulle optimointiongelmalle / Find the Karush–Kuhn–Tucker conditions of the constrained optimization problem

$$
\min \; e^{x_1} + e^{x_2}
$$

**rajoitteella / subject to the constraint**  
$$
x_1 + x_2 \geq 4.
$$

Määritä optimaalisen pisteen approksimaatio Uzawan algoritmilla. Käytä duaalin alkuarvauksena $u_0 = 7.5$ ja askelpituutena $\rho = 4$. Laske kaksi iteraatiota.  
Solve an approximation to the optimal point using Uzawa’s algorithm with the initial value for the dual variable $u_0 = 7.5$ and the step length $\rho = 4$. Compute two iterations.